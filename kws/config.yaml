# config.yaml

seed: 42
device: auto              # auto / cpu / cuda

main_dir: "C:\\temp\\KWS\\data\\speech-commands-v2"
folder_start: 13
folder_end: 20
sampling_rate: 16000

n_mfcc: 13
use_parallel: true

train_ratio: 0.8
val_ratio: 0.1
test_ratio: 0.1
random_state: 42

batch_size: 64
num_workers: 0
shuffle_train: true

epochs: 150
patience: 10
lr: 0.001
weight_decay: 0.001

train_basic: true
train_improved: true

make_confusion: true
show_plots: true

mis_enabled: true
mis_true_label: "learn"
mis_pred_label: "house"
mis_max_rows: 50
